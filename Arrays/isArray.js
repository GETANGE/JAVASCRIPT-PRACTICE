//this method is used to test whether the value passed is an array.
let numbers=[1,2,3,4,5]
console.log(Array.isArray(numbers));//returns true 

let text='Javascript'
console.log(Array.isArray(text));//returns false.